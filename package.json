{
  "name": "dcl-project",
  "version": "1.0.0",
  "description": "My new Decentraland project",
  "scripts": {
    "start": "dcl start",
    "build": "build-ecs",
    "watch": "build-ecs --watch",
    "deploy:now": "now --platform-version 1 --docker",
    "ecs:install-next": "npm install --save-dev decentraland-ecs@next",
    "ecs:install-stable": "npm install --save-dev decentraland-ecs@latest"
  },
  "devDependencies": {
    "decentraland-ecs": "^1.0.1-20181220152036.commit-d560183"
  }
}
